"""Application layer package.

This layer contains use cases (application business rules), DTOs for data transfer,
and port interfaces for external dependencies. It orchestrates the domain layer
but remains independent of infrastructure details.
"""
