// Ensure minimal module declarations are picked up by TypeScript.
// Import-style inclusion preferred over triple-slash references so ESLint and
// modern TS toolchains can correctly resolve and validate the declarations.
import './types/diff';

// Additional global shims can go here in future.

export {};
