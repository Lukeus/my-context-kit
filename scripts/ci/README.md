# CI Scripts

- `run-gates.ts`: Generates FR-040 gating artifact. Replace stub statuses with real command executions.

Future enhancements:
- Spawn validation (`pnpm validate`) & capture duration.
- Collect typecheck, lint, and test results.
- Integrate embeddings checksum from pipeline.
